# Python Requirements for Tomb of the Unknowns Analysis
# Install with: pip install -r requirements.txt

# Core data science
numpy>=2.4.6
scikit-learn>=1.9.0
pyyaml>=6.0.3

# NLP and Sentiment
spacy>=3.8.14
vaderSentiment>=3.3.2
textblob>=0.20.0

# Visualization (if generating static images)
matplotlib>=3.11.0
plotly>=6.8.0

# PDF Processing
pdfplumber>=0.11.10    # Layout-aware text extraction
PyMuPDF>=1.27.2.3       # Font metadata for heading detection (import as fitz)

# Utilities
tqdm>=4.68.2

# Web Interface
streamlit>=1.58.0
watchdog>=6.0.0       # For Streamlit file watching

# Desktop Application (optional - for standalone app)
pywebview>=6.2.1      # Native window wrapper for desktop app

# Note: For spaCy models, run after installation:
# python -m spacy download en_core_web_trf  (best accuracy)
# python -m spacy download en_core_web_sm   (faster, smaller)
